# نقطة الاستئناف — نظام طلبات الصرف

## ✅ مطبّق على Supabase (ثابت) — مشروع mwppgpllmtxfqxmgmyju
migrations في مجلد `database/`:
- 01 الإشعارات + status_changed_at + role_for_stage
- 02 دور admin + current_role_of()
- 03 SLA (sla_settings enabled=false افتراضيًا) + generate_delay_alerts + pg_cron كل ساعة
- 04 سجل التدقيق request_history
- 05 تسجيل تعديلات موظف الحسابات (action='edited')
- 06 is_active على profiles + سياسة تعديل الأدمن + request_no صار GENERATED BY DEFAULT
     + دالة admin_set_request_start(p_next)

**Edge Functions منشورة (عبر MCP، ACTIVE):**
- `create-user` (أدمن فقط: ينشئ مستخدم بإيميل+كلمة مرور+اسم+دور)
- `admin-set-password` (أدمن فقط: إعادة تعيين كلمة مرور)
- SUPABASE_URL و SUPABASE_SERVICE_ROLE_KEY متوفّران تلقائيًا لهما (لا secrets يدوية).

**بيانات:** 562 طلب (request_no 136–697). SLA مطفأة عمدًا.

## ✅ في index.html (غير منشور — يرفعه المالك يدويًا على GitHub)
الإشعارات (جرس) · عمود «منذ» + شارة «متأخر» · الخط الزمني (سجل التدقيق) في التفاصيل ·
تعديل الطلب لموظف الحسابات · لوحة مؤشرات بالرسوم + فلتر شهور · **تبويب «الإدارة»**
(للأدمن فقط: المستخدمون + محرّر SLA + ضبط بداية الترقيم) · فحص is_active عند الدخول.
تم التحقق: node --check سليم، divs متوازنة.

## 🔴 بلوكرات / خطوات المالك
1. **النشر:** رفع أحدث `index.html` على GitHub (المالك يرفع يدويًا؛ الـpush التلقائي
   محجوب بصلاحية قراءة فقط). 8+ commits محليًا على فرع claude/project-handoff-verification-m0jctj.
2. **تهيئة الأدمن:** المالك ينشئ `admin@aldeyabi.com` + كلمة مرور من Supabase Auth،
   ثم أشغّل: `update public.profiles set role='admin', is_active=true where email='admin@aldeyabi.com';`
3. **المرحلة د:** الرقم المسلسل الحقيقي الذي وصلت له الإدارة المالية (لضبط بداية الترقيم؛
   يجب أن يكون > 697). يُضبط من تبويب الإدارة → «بداية ترقيم الطلبات».

## ⏭️ متبقٍّ (تحسينات اختيارية — task 8)
- doc_type للمرفقات (تصنيف)
- سبب تأخير إلزامي عند التصرّف في طلب متأخر
- ترقيم DEY-YYYY-NNN (اختياري)

## خطوات التشغيل الأولى للأدمن بعد الدخول
تبويب «الإدارة» → أنشئ باقي المستخدمين · اضبط مدد SLA وفعّلها · اضبط بداية الترقيم.
